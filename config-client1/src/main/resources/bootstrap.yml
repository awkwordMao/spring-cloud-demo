spring:
  application:
    name: config
  cloud:
    config:
      label: master
      profile: dev
      discovery:
        enabled: true
        service-id: config-service
    bus:
      trace:
        enabled: true
#      uri: http://localhost:4002/
server:
  port: 4022
eureka:
  client:
    service-url:
      defaultZone: http://localhost:4001/eureka/